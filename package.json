{
  "name": "markdown-them",
  "displayName": "Markdown Them",
  "description": "Convert docx, pdf, html, xlsx, pptx, odt, odp, ods, rtf files to markdown in your VS Code's explorer or editor.",
  "version": "1.1.4",
  "icon": "assets/markdown-them-logo.png",
  "publisher": "the-long-ride",
  "repository": {
    "type": "git",
    "url": "https://github.com/the-long-ride/vscode-extension-markdown-them.git"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other",
    "Formatters"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "markdown-them.convertToMarkdown",
        "title": "Convert to Markdown"
      },
      {
        "command": "markdown-them.convertCurrentToMarkdown",
        "title": "Convert Current File to Markdown"
      },
      {
        "command": "markdown-them.setMaxConcurrentConversions",
        "title": "Markdown Them: Set Max Concurrent Conversions"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "markdown-them.convertToMarkdown",
          "group": "navigation",
          "when": "resourceExtname =~ /\\.(docx|pdf|html|xlsx|pptx|odt|odp|ods|rtf)$/i"
        }
      ]
    },
    "keybindings": [
      {
        "command": "markdown-them.convertCurrentToMarkdown",
        "key": "ctrl+m ctrl+d",
        "mac": "cmd+m cmd+d"
      }
    ],
    "configuration": {
      "title": "Markdown Them",
      "properties": {
        "markdown-them.maxConcurrentConversions": {
          "type": "integer",
          "default": 6,
          "minimum": 1,
          "maximum": 16,
          "description": "Maximum number of files converted simultaneously when using 'Convert to Markdown'. Range: 1–16."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run bundle",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "bundle": "esbuild ./src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --minify",
    "publish": "npm run bundle && vsce package"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/turndown": "^5.0.6",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "@opendocsg/pdf2md": "^0.2.6",
    "exceljs": "^3.4.0",
    "mammoth": "^1.12.0",
    "officeparser": "^6.1.0",
    "turndown": "^7.2.4"
  },
  "resolutions": {
    "fast-uri": "^3.1.2",
    "tmp": "^0.2.4",
    "fast-csv": "^4.3.6"
  }
}
